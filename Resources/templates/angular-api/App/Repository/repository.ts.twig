{% autoescape false %}
{% set className = definitionName|class_name %}
import { Injectable } from '@angular/core';
import { Repository } from '../Repository';
import { AppHttp } from '../AppHttp';
import {{ '{ ' ~ className ~ ' }' }} from '../Model/{{ className }}';

@Injectable()
export class {{ className }}Repository extends Repository<{{ className }}>
{
    constructor(protected appHttp:AppHttp) {
        super(appHttp, 'users');
    }

    public newInstance():{{ className }} {
        return new {{ className }}();
    }
}
{% endautoescape %}